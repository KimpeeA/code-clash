class_name EntityStats
extends Resource

@export_range(0, 600) var walk_speed: float
@export_range(0, 600) var run_speed: float

@export_range(0, 600) var dash_distance: float
@export_range(0, 600) var dash_duration: float

@export_range(0, 600) var jump_force: float
